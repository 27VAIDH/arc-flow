{
  "project": "ArcFlow",
  "branchName": "ralph/save-to-workspace",
  "description": "Save items individually to workspace without folders — loose saved items at workspace root level with full drag-and-drop support",
  "techContext": {
    "framework": "react-chrome-extension",
    "language": "typescript",
    "orm": "chrome.storage.local",
    "uiLibrary": "tailwind-v4",
    "testFramework": "vitest",
    "commands": {
      "typecheck": "npm run typecheck",
      "test": "npm run test:run",
      "dev": "npm run dev",
      "lint": "npm run lint"
    }
  },
  "userStories": [
    {
      "id": "US-001",
      "title": "Add savedItems field to Workspace data model",
      "description": "Add a savedItems: FolderItem[] array to the Workspace interface and create a schema migration (V3 → V4) that initializes the field to [] on all existing workspaces. Update createDefaultWorkspace(), createWorkspace(), and createWorkspaceFromTemplate() to include savedItems: [].",
      "dependsOn": [],
      "filesToModify": [
        { "path": "src/shared/types.ts", "action": "Add savedItems: FolderItem[] to Workspace interface" },
        { "path": "src/shared/workspaceStorage.ts", "action": "Add migrateToV4(), bump CURRENT_SCHEMA_VERSION to 4, update ensureInitialized(), createDefaultWorkspace(), createWorkspace(), createWorkspaceFromTemplate()" }
      ],
      "filesToRead": [
        { "path": "src/shared/workspaceStorage.ts", "reason": "Follow migrateToV3() pattern for the new migration" },
        { "path": "src/shared/types.ts", "reason": "Understand current Workspace interface shape" }
      ],
      "acceptanceCriteria": [
        "Workspace interface has savedItems: FolderItem[] field",
        "CURRENT_SCHEMA_VERSION bumped to 4",
        "migrateToV4() adds savedItems: [] to all existing workspaces where undefined",
        "ensureInitialized() calls migrateToV4() when version < 4",
        "createDefaultWorkspace() includes savedItems: []",
        "createWorkspace() includes savedItems: [] and clones savedItems when cloning",
        "createWorkspaceFromTemplate() includes savedItems: []",
        "npm run typecheck passes",
        "npm run test:run passes"
      ],
      "priority": 1,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-002",
      "title": "Add workspace-scoped savedItems CRUD functions",
      "description": "Add storage functions for managing loose saved items: addSavedItemToWorkspace, removeSavedItemFromWorkspace, renameSavedItemInWorkspace, reorderSavedItemsInWorkspace, moveSavedItemToFolderInWorkspace, moveFolderItemToSavedItemsInWorkspace. Add facade wrappers in folderStorage.ts.",
      "dependsOn": ["US-001"],
      "filesToModify": [
        { "path": "src/shared/workspaceStorage.ts", "action": "Add 6 new exported CRUD functions for savedItems" },
        { "path": "src/shared/folderStorage.ts", "action": "Add facade wrappers that get active workspace ID and delegate" }
      ],
      "filesToRead": [
        { "path": "src/shared/workspaceStorage.ts", "reason": "Follow addItemToFolderInWorkspace / removeItemFromFolderInWorkspace / moveFolderItemToFolderInWorkspace patterns" },
        { "path": "src/shared/folderStorage.ts", "reason": "Follow existing facade pattern (get wsId, delegate)" }
      ],
      "acceptanceCriteria": [
        "addSavedItemToWorkspace(workspaceId, item) adds item to ws.savedItems",
        "removeSavedItemFromWorkspace(workspaceId, itemId) removes item from ws.savedItems",
        "renameSavedItemInWorkspace(workspaceId, itemId, newTitle) renames a saved item",
        "reorderSavedItemsInWorkspace(workspaceId, orderedItemIds) reorders saved items",
        "moveSavedItemToFolderInWorkspace(workspaceId, itemId, targetFolderId) moves item from savedItems to a folder's items",
        "moveFolderItemToSavedItemsInWorkspace(workspaceId, itemId) finds item across all folders, removes it, adds to savedItems",
        "Facade functions in folderStorage.ts: addSavedItem, removeSavedItem, renameSavedItem, reorderSavedItems, moveSavedItemToFolder, moveFolderItemToSavedItems",
        "npm run typecheck passes",
        "npm run test:run passes"
      ],
      "priority": 2,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-003",
      "title": "Render loose saved items below folder tree in sidebar",
      "description": "Update App.tsx to load and manage savedItems state from the active workspace. Create SavedItemsList.tsx component rendered below FolderTree showing loose saved items with favicon + title, click to open, and sortable via @dnd-kit. Show 'Saved' section label when items exist.",
      "dependsOn": ["US-002"],
      "filesToModify": [
        { "path": "src/sidepanel/App.tsx", "action": "Add savedItems state, load from workspace, render SavedItemsList below FolderTree" }
      ],
      "filesToCreate": [
        { "path": "src/sidepanel/SavedItemsList.tsx", "action": "New component rendering sortable list of FolderItem entries with drag, click, context menu, inline rename" }
      ],
      "filesToRead": [
        { "path": "src/sidepanel/FolderTree.tsx", "reason": "Follow DraggableFolderItem component pattern for item rendering" },
        { "path": "src/sidepanel/App.tsx", "reason": "Follow how folders state is loaded and managed alongside workspace switching" }
      ],
      "acceptanceCriteria": [
        "savedItems state loaded from activeWorkspace.savedItems on workspace switch",
        "savedItems state synced via chrome.storage.onChanged",
        "SavedItemsList component renders below FolderTree when savedItems.length > 0",
        "Each item shows favicon + title (truncated), click opens URL in new tab",
        "Items are sortable via @dnd-kit (drag to reorder within saved items list)",
        "Visual style matches DraggableFolderItem (same height, padding, hover state)",
        "Section label 'Saved' shown above items (same style as 'Folders' label in FolderTree)",
        "npm run typecheck passes",
        "npm run test:run passes"
      ],
      "priority": 3,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-004",
      "title": "Add 'Save to Workspace' context menu option for tabs",
      "description": "Add a 'Save to Workspace' option to the tab context menu (handleTabContextMenu) that saves the tab as a loose item directly — no folder picker needed. Always visible regardless of folder count. Duplicate check against savedItems. Toast confirmation.",
      "dependsOn": ["US-002"],
      "filesToModify": [
        { "path": "src/sidepanel/App.tsx", "action": "Add 'Save to Workspace' menu item in handleTabContextMenu, import addSavedItem, add duplicate check against savedItems" }
      ],
      "filesToRead": [
        { "path": "src/sidepanel/App.tsx", "reason": "Follow handleTabContextMenu pattern at ~line 1633" }
      ],
      "acceptanceCriteria": [
        "'Save to Workspace' menu item appears in tab context menu (always visible, no folder requirement)",
        "Clicking it creates a FolderItem with type 'link' and saves via addSavedItem()",
        "Duplicate check: if item with same URL exists in savedItems, show toast 'Already saved in workspace' and skip",
        "'Save Link to Folder...' still appears when folders exist (unchanged behavior)",
        "Toast confirmation shown: 'Saved to workspace'",
        "npm run typecheck passes",
        "npm run test:run passes"
      ],
      "priority": 4,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-005",
      "title": "Drag-and-drop between loose items and folders",
      "description": "Update the drag-and-drop system in App.tsx to support: tab→saved-items (save as loose item), saved-item→folder-drop (move to folder), folder-item→saved-items-drop (make loose), saved-item reorder. Add saved-items-drop droppable zone and update customCollisionDetection.",
      "dependsOn": ["US-003"],
      "filesToModify": [
        { "path": "src/sidepanel/App.tsx", "action": "Update handleDragEnd with new drag cases, update customCollisionDetection to include saved-items-drop" },
        { "path": "src/sidepanel/SavedItemsList.tsx", "action": "Add useDroppable for saved items area with id saved-items-drop" }
      ],
      "filesToRead": [
        { "path": "src/sidepanel/App.tsx", "reason": "Follow handleDragEnd cases (tab→folder-drop ~line 1929, folder-item→folder-drop ~line 2075) and customCollisionDetection pattern" }
      ],
      "acceptanceCriteria": [
        "Dragging a tab onto saved items area saves it as a loose FolderItem (with duplicate check)",
        "Dragging a saved-item:* onto a folder-drop:* target moves it from savedItems to that folder",
        "Dragging a folder-item:* onto saved-items-drop moves it from its folder to savedItems",
        "Dragging saved items within the list reorders them",
        "customCollisionDetection recognizes saved-items-drop as a valid target",
        "Drop indicator shown when hovering over saved items area",
        "npm run typecheck passes",
        "npm run test:run passes"
      ],
      "priority": 5,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-006",
      "title": "Context menu and inline rename for loose saved items",
      "description": "Add right-click context menu for loose saved items with options: Open in New Tab, Rename, Move to Folder... (opens folder picker), Delete. Support inline rename on double-click matching DraggableFolderItem pattern.",
      "dependsOn": ["US-003", "US-004"],
      "filesToModify": [
        { "path": "src/sidepanel/App.tsx", "action": "Add handleSavedItemContextMenu and handleSavedItemRename callbacks, wire folder picker for Move to Folder action" },
        { "path": "src/sidepanel/SavedItemsList.tsx", "action": "Wire onContextMenu and onRename props to each item" }
      ],
      "filesToRead": [
        { "path": "src/sidepanel/App.tsx", "reason": "Follow handleTabContextMenu pattern for building context menu items" },
        { "path": "src/sidepanel/FolderTree.tsx", "reason": "Follow DraggableFolderItem inline rename pattern" }
      ],
      "acceptanceCriteria": [
        "Right-click on saved item shows context menu with: Open in New Tab, Rename, Move to Folder..., Delete",
        "Open in New Tab sends OPEN_URL message to service worker",
        "Rename triggers inline rename mode (same as folder item double-click behavior)",
        "Move to Folder... opens existing FolderPickerDropdown and moves item to selected folder",
        "Delete removes item from savedItems with no confirmation",
        "Double-click on item title enters inline rename mode",
        "npm run typecheck passes",
        "npm run test:run passes"
      ],
      "priority": 6,
      "passes": false,
      "notes": ""
    }
  ]
}
